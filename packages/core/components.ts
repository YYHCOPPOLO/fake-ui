import type { Plugin } from 'vue'

import {
  FakeButton,
  FakeButtonGroup,
  FakeCollapse,
  FakeCollapseItem,
  FakeIcon,
} from '@fake-ui/components'

export default [
  FakeButton,
  FakeButtonGroup,
  FakeIcon,
  FakeCollapse,
  FakeCollapseItem,
] as Plugin[]
