import { withInstall } from '@fake-ui/utils'
import Button from './Button.vue'
import ButtonGroup from './ButtonGroup.vue'

export const FakeButton = withInstall(Button)
export const FakeButtonGroup = withInstall(ButtonGroup)